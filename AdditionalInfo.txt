// Start the program:
To run the program on Linux, type: g++ main.cpp -std=c++11
When it is built, type: ./a.out

// When you test the program:
If you want to see the full picture of what is going on in simulation during the runtime of the program, run the command "show all". This command combines "S r", "S m", and "S i" together.
If you want to stop simulation, run command "QUIT".
